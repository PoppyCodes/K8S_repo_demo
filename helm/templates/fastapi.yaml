apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.fastapi.name }}-deployment
  namespace: {{ .Values.namespace }}
  labels:
    name: {{ .Values.fastapi.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.fastapi.name}}
  replicas: 3
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: {{ .Values.fastapi.name }}
    spec:
      containers:
        - name: {{ .Values.fastapi.name}}
          image: {{ .Values.fastapi.image }}
          ports:
            - containerPort: 5000

---

apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.fastapi.name }}-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.fastapi.name }}-deployment
  minReplicas: {{ .Values.fastapi_hpa.minreplicas }}
  maxReplicas: {{ .Values.fastapi_hpa.maxreplicas }}
  targetCPUUtilizationPercentage: {{ .Values.fastapi_hpa.targetCPUUtilizationPercentage }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fastapi.name }}-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.fastapi.name }}
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.fastapi_service.clusteripport }}
      targetPort: {{ .Values.fastapi.port}}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.fastapi_certificate.name }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.fastapi_certificate.secretname }}
  issuerRef:
    name: {{ .Values.fastapi_certificate.issuername }}
    kind: ClusterIssuer
  dnsNames:
  - {{ .Values.fastapi_certificate.dnsnames }}
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.fastapi.name}}-ingress
  namespace: {{ .Values.namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.fastapi_certificate.issuername }}
spec:
  ingressClassName: traefik
  tls:
    - secretName: letsencrypt-prod
      hosts:
      - {{ .Values.fastapi_ingress.host }}
  rules:
  - host: {{ .Values.fastapi_ingress.host }}
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
              service:
                name: {{ .Values.fastapi.name }}-service
                port:
                  number: {{ .Values.fastapi_ingress.port }}